<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head/_head', { page: user.username }) %>

    <body>
        <%- include('../partials/navbar/_navbar') %>
        <main>
            <!-- <div>
                <h1>Homepage</h1>
                <h2>Username: <%= user.username %></h2>
               
                <p>DOB: <%= user.dob %></p>
                <p>Email: <%= user.email %></p>
            </div> -->
            <article class="user-queue-index">
                <h1 class="index-header">MY QUEUE</h1>
                <div class="index-boxes-container">
                    <% for (let i = 0; i < user.showQueue.length; i++) { %>
                        <% const queue = user.showQueue[i] %>
                        <div class="index-boxes">
                        <a href="/shows/<%=queue._id%>">
                            <img class="thumbnail" src="<%=queue.image%>">
                            <p><%=queue.name%></p>
                        </a>
                        <form action="/users/<%= user.id %>?_method=PUT" method="POST">
                            <input hidden name="remove" value="<%= queue._id %>" />
                            <button type="submit">Remove from queue</button>
                        
                        </form>
                        </div>
                    <%}%>
                </div>
                </article>
           <!--  <div>
                <h2>Liked Episodes:</h2>
                <div class="">
                    % for (let i = 0; i < user.episodeLikes.length; i++) { %>
                        % const like = user.episodeLikes[i];
                        console.log(user.episodeLikes); %>
                        <div class="">
                        <a href="/episodes/%= like._id %>">
                            <img class="thumbnail" src="%=like.image%>">
                            <p>%=like.name%></p>
                        </a>
                        <form action="/users/%= user.id %>?_method=PUT" method="POST">
                            <input hidden name="remove" value="%= like._id %>" />
                            <button type="submit">Unlike</button>
                        
                        </form>
                        </div>
                    %}%>
                </div>
            </div> -->
        </main>
    </body>

</html>